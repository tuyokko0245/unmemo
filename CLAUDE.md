@AGENTS.md

# 不具合対応ワークフロー

不具合の報告を受けたら、以下のフェーズを順番に実行する。

## Phase 1: 調査

コードに触れる前に必ず実施する。

- `git status` で現在の変更状態を確認
- 報告された症状に関係するファイル・関数を特定
- 呼び出し経路とデータの流れを追う
- 類似の実装パターンを探す

推測だけで原因を決定しない。

## Phase 2: 診断

以下を明確にしてからユーザーに報告する。

- **Problem**: 何が起きているか
- **Root Cause**: なぜ起きているか（コード上の根拠を示す）
- **Fix Direction**: どう直すか（方向性のみ）

## Phase 3: 判断

以下の場合はユーザーに確認してから進む。

- 仕様の判断が必要
- 影響範囲が広い
- 外部サービス・設定ファイルの変更が必要
- Firebase・環境変数・Vercel設定の変更が必要

それ以外はそのまま実装に進む。

## Phase 4: 実装

- 既存のコードパターンに従う
- 最小限の変更にする
- 無関係なリファクタリングをしない
- 新しいライブラリを勝手に追加しない

## Phase 5: 検証

変更後に以下を順番に実行する。

```powershell
# Lintチェック
npm run lint

# ビルドチェック（型エラーも検出される）
npm run build
```

エラーがあれば自分の変更が原因か確認して修正する。

## Phase 6: 報告とコミット確認

以下の形式でユーザーに報告する。

### Root Cause
根本原因。

### Changes
変更したファイルと内容。

### Tests
実行した検証と結果。

### Remaining Issues
残っている問題（なければ「なし」）。

---

報告後、以下のコミットメッセージ案を提示してユーザーに確認を取る。

```
fix: （修正内容を一行で）

- 根本原因の概要
- 変更したファイル
```

ユーザーが承認したらcommitとpushを実行する。

## 禁止事項

- main/masterへのforce push
- `git reset --hard`
- `git clean -fd`
- ユーザーの承認なしにcommit・pushしない
- `.env.local` など環境変数ファイルの変更はユーザーに確認する
